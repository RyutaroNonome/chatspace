%li.message
  .message__information
    %span.name
      = message.user.name
    %span.time
      / = message.created_at.strftime('%Y/%m/%d %H:%M:%S')
      = message.created_at.to_s(:default)
  %p.message__body
    = message.body